{
  "name": "rushia-jour-02",
  "version": "1.0.0",
  "description": "Todolist HTML/CSS/JS avec Parcel",
  "source": "index.html",
  "scripts": {
    "dev": "parcel --open",
    "build": "parcel build --public-url ./",
    "start": "parcel --open"
  },
  "devDependencies": {
    "parcel": "^2.12.0"
  },
  "targets": {
    "default": {
      "distDir": "./dist"
    }
  }
}

